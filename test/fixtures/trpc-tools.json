[
  {
    "name": "create_customer",
    "description": "Create a new customer",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "age": { "type": "integer" }
      },
      "required": ["id", "email"]
    }
  },
  {
    "name": "create_order",
    "description": "Create a new order",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "total": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "shipped", "delivered"] }
      },
      "required": ["id", "total"]
    }
  },
  {
    "name": "product_create",
    "description": "Create a new product (suffix pattern, camelCase schema key)",
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "sku": { "type": "string" },
        "price": { "type": "number" }
      },
      "required": ["id", "sku"]
    }
  },
  {
    "name": "tag_add",
    "description": "Add a tag (suffix pattern, array field)",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "label": { "type": "string" },
        "aliases": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["id", "label"]
    }
  },
  {
    "name": "infos_customers_create",
    "description": "Create a customer (namespaced — should yield Customer, not InfosCustomers)",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "name": { "type": "string" }
      },
      "required": ["id"]
    }
  },
  {
    "name": "infos_widgets_create",
    "trpcPath": "widgets.create",
    "description": "Create a widget (has trpcPath field)",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "name": { "type": "string" }
      },
      "required": ["id"]
    }
  },
  {
    "name": "get_customer",
    "description": "Get a customer by ID",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": { "type": "string" }
      },
      "required": ["id"]
    }
  },
  {
    "name": "delete_customer",
    "description": "Delete a customer",
    "input_schema": {
      "type": "object",
      "properties": {
        "id": { "type": "string" }
      },
      "required": ["id"]
    }
  }
]
